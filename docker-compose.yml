version: '3.8'

services:
  app:
    build: .
    image: restaurant-review:0.0.1-SNAPSHOT
    container_name: restaurant-review-container
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/reviewdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
      - SPRING_DATASOURCE_USERNAME=admin
      - SPRING_DATASOURCE_PASSWORD=password

  db:
    image: mysql:8.0
    container_name: restaurant-review-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root_password
      - MYSQL_DATABASE=reviewdb
      - MYSQL_USER=admin
      - MYSQL_PASSWORD=password
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./databaseChangeLog.yml:/docker-entrypoint-initdb.d/databaseChangeLog.yml

volumes:
  db_data: